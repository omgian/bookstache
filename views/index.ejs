<%- include('partials/header', {admin: false}) -%>
<div class="container favs">
  <h3>Yoan's Current Favorite Books</h3>

  <div id="carouselExampleCaptions" class="carousel slide" data-bs-ride="carousel">
    <div class="carousel-indicators">
    <% for(var i=0; i<favBooks.length; i++) {%>
      <%if (i === 0) { %>
        <button type="button" data-bs-target="#carouselExampleCaptions" data-bs-slide-to="<%= i%>" class="active" aria-current="true" aria-label="Slide <%= i%>"></button>
      <% } else {%>
        <button type="button" data-bs-target="#carouselExampleCaptions" data-bs-slide-to="<%= i%>" aria-label="Slide <%= i%>"></button>
      <% } %>
    <% } %>
    </div>
    <div class="carousel-inner">

    <% for(var i=0; i<favBooks.length; i++) {%>
      <%if (i === 0) { %>
      <div class="carousel-item active">
      <% } else {%>
        <div class="carousel-item">
      <% } %>
        <img src="<%= favBooks[i].image%>" class="d-block w-100 carousel-bookcover" alt="book cover for <%= books[i].title%>">
        <div class="carousel-caption d-none d-md-block">
          <h2><%= favBooks[i].title%></h2>
          <p><%= favBooks[i].author%></p>
          <p><%= favBooks[i].description.slice(0,150)%>
          
          <%if (favBooks[i].description.length > 150) { %>
            ...
          <% } %>
          </p>
        </div>
      </div>
    <% } %>
    </div>
    <button class="carousel-control-prev" type="button" data-bs-target="#carouselExampleCaptions" data-bs-slide="prev">
      <span class="carousel-control-prev-icon" aria-hidden="true"></span>
      <span class="visually-hidden">Previous</span>
    </button>
    <button class="carousel-control-next" type="button" data-bs-target="#carouselExampleCaptions" data-bs-slide="next">
      <span class="carousel-control-next-icon" aria-hidden="true"></span>
      <span class="visually-hidden">Next</span>
    </button>
  </div>
</div>
<div class="container">
  <div class="input-group mb-3">
    <input type="text" class="form-control" value="<%= search%>" placeholder="Search" aria-label="Search" aria-describedby="button-addon2">
    <button class="btn btn-outline-secondary" type="button" id="button-addon2">Search</button>
  </div>
</div>
<div class="container">
  <div class="row row-cols-lg-4">
    <% for(var i=0; i<books.length; i++) {%>
    <div class="col-lg">
      <div class="card border-light text-center" style="width: 18rem;">
        <div class="text-center">
          <img src="<%= books[i].image%>" class="bookcover" alt="book cover for <%= books[i].title%>">
        </div>
        <div class="card-body">
          <p class="card-text title"><%= books[i].title%></p>
          <p class="card-text author"><%= books[i].author%></p>
        </div>
      </div>
    </div>
    <% } %>
  </div>
</div>
<%- include('partials/footer') -%>









<!-- <div class="container">
  <main class="container">
    <div class="row justify-content-around mt-5">
        <a href="/login" class="col-3 btn btn-primary"> Login</a> -->
<!-- <a href="/signup" class="col-3 btn btn-primary"> Signup</a> -->
<!-- </div> -->
<!-- <div class="row mt-5">
      <div class="col-6">
        <ul class="row list-unstyled">
        <% for(var i=0; i<books.length; i++) {%>
          <li class="col-6 justify-content-between mt-5">
              <img class="img-fluid" src="<%= books[i].image%>">
              <span><%= books[i].title %></span>
              <span><%= books[i].author %></span> -->
<!-- <span><%= books[i].description %></span> -->
<!--               
          </li>
        <% } %>
        </ul>
      </div>  
    </div>
  </div>
</div> -->